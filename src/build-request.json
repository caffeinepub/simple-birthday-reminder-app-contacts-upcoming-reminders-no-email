{
  "kind": "build_request",
  "title": "Publish Draft Version 11 live",
  "priority": "high",
  "requirements": [],
  "constraints": [
    "Publish the current draft build to a live (production) Internet Computer canister with no functional changes to the app.",
    "Use English for any user-facing text."
  ],
  "nonGoals": [
    "Adding or modifying application features (contacts, upcoming birthdays, gift plans).",
    "Changing backend data schema or performing state migrations unless strictly required by deployment."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}